version: '3.8'
services:
  task-service:
    build: ./task-service
    ports:
      - "5001:80"
  ai-service:
    build: ./ai-service
    ports:
      - "5002:8080"
  api-gateway:
    build: ./api-gateway
    ports:
      - "5000:80"
    depends_on:
      - task-service
      - ai-service
